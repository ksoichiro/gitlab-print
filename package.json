{
  "name": "gitlab-print",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf build",
    "build": "mkdir -p build/extension && cp -pR src icons manifest.json license build/extension/ && cd build && zip -r extension.zip extension"
  }
}
